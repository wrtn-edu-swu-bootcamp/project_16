# TweetLingo Environment Variables
# Copy this file to .env.local and fill in your actual values

# ==============================================================================
# DATABASE (Cloud-based - No Docker required)
# ==============================================================================
# Choose one of the following cloud PostgreSQL providers (all have free tiers):
#
# Option 1: Vercel Postgres (Recommended for Vercel deployment)
#   - Create at: https://vercel.com/dashboard → Storage → Create Database → Postgres
#   - Vercel will auto-populate these in production
#
# Option 2: Neon (Recommended for local development)
#   - Create at: https://neon.tech
#   - Copy connection string from dashboard
#
# Option 3: Supabase
#   - Create at: https://supabase.com
#   - Settings → Database → Connection string
#
# Example format:
DATABASE_URL="postgresql://user:password@host:5432/database?sslmode=require"
DIRECT_URL="postgresql://user:password@host:5432/database?sslmode=require"

# ==============================================================================
# AUTHENTICATION
# ==============================================================================
# Generate with: [Convert]::ToBase64String((1..32 | ForEach-Object { Get-Random -Maximum 256 }))
# Must be at least 32 characters
AUTH_SECRET="your-auth-secret-here-minimum-32-characters-long"

# Local: http://localhost:3000
# Production: https://your-app.vercel.app
AUTH_URL="http://localhost:3000"

# ==============================================================================
# GOOGLE OAUTH (for user login)
# ==============================================================================
# Get from: https://console.cloud.google.com/apis/credentials
# 1. Create new project
# 2. Create OAuth 2.0 Client ID
# 3. Add redirect URI: http://localhost:3000/api/auth/callback/google (local)
#                       https://your-app.vercel.app/api/auth/callback/google (production)
GOOGLE_CLIENT_ID="your-google-client-id.apps.googleusercontent.com"
GOOGLE_CLIENT_SECRET="your-google-client-secret"

# ==============================================================================
# GEMINI API (for AI word extraction)
# ==============================================================================
# Get from: https://ai.google.dev/
# Or: https://console.cloud.google.com/apis/credentials
# Enable "Generative Language API" first
GEMINI_API_KEY="your-gemini-api-key"

# ==============================================================================
# X/TWITTER API (OPTIONAL - Not required!)
# ==============================================================================
# TweetLingo works WITHOUT X API by using:
#   - Text input: Users paste tweet text directly
#   - Chrome Extension: Extracts text from X pages automatically
#
# X API is only needed if you want URL-based analysis (not recommended due to costs)
# Free tier only allows 100 tweets/month, and read access may be limited
#
# If you still want to use X API:
# 1. Create developer account at https://developer.x.com
# 2. Create new app
# 3. Generate Bearer Token
#
# Leave empty if not using X API (recommended):
X_API_BEARER_TOKEN=""

# ==============================================================================
# NOTION INTEGRATION (optional - for Notion sync)
# ==============================================================================
# Get from: https://www.notion.so/my-integrations
# 1. Create new integration
# 2. Set up OAuth (Public integration)
# 3. Add redirect URI: http://localhost:3000/api/notion/callback (local)
#                       https://your-app.vercel.app/api/notion/callback (production)
NOTION_CLIENT_ID="your-notion-client-id"
NOTION_CLIENT_SECRET="your-notion-client-secret"

# ==============================================================================
# ENCRYPTION (for storing sensitive data)
# ==============================================================================
# Generate with: -join ((0..31 | ForEach-Object { '{0:x2}' -f (Get-Random -Maximum 256) }))
# Must be exactly 64 hex characters (32 bytes)
ENCRYPTION_KEY="your-64-character-hex-encryption-key-here-0123456789abcdef"

# ==============================================================================
# APPLICATION URL
# ==============================================================================
# Local: http://localhost:3000
# Production: https://your-app.vercel.app
NEXT_PUBLIC_APP_URL="http://localhost:3000"
